// Example: src/pages/HomePage.js
import Button from '../components/Button'; // Adjust the path based on your folder structure

const HomePage = () => {
  return (
    <div>
      <h1>Welcome to the Home Page</h1>
      <Button>Click Me</Button>
    </div>
  );
};

export default HomePage;
